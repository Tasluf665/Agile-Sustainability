{
  "name": "agile-sustainability-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "if [ \"$PROJECT\" = \"frontend\" ]; then npm run start:frontend; else npm run start:backend; fi",
    "heroku-postbuild": "if [ \"$PROJECT\" = \"frontend\" ]; then npm run build:frontend; else npm run build:backend; fi",
    "start:backend": "cd backend && node server.js",
    "build:backend": "cd backend && npm install",
    "start:frontend": "cd frontend && npx serve -s dist -l $PORT",
    "build:frontend": "cd frontend && npm install && npm run build"
  }
}
